# 01 - Paradigma Imperativo (Pascal)
## Foco: Modularização, Funções e Conversão de Linguagem (Lista 3)

Este módulo foca na aplicação avançada do Paradigma Imperativo usando a linguagem **Pascal**, com ênfase na **Modularização** e na **conversão lógica** de código entre diferentes linguagens (C para Pascal).

### 🎯 Objetivos Implementados (Lista 3)

O principal objetivo desta lista foi demonstrar o domínio sobre a criação de sub-rotinas e o gerenciamento de variáveis em escopos modulares:

* **Modularização de Código:**
    * Uso de `PROCEDURE` para rotinas que não retornam valor (Ex: rotinas de I/O ou processamento).
    * Uso de `FUNCTION` para rotinas que retornam um valor único (Ex: cálculo de valor menor entre três números).
* **Passagem de Parâmetros:**
    * Domínio da **passagem por referência** utilizando a palavra-chave `VAR` para permitir que sub-rotinas alterem o valor original de uma variável.
* **Conversão de Linguagem (Problema 1):**
    * Implementação do algoritmo de **cálculo de potência (base^expoente)**.
    * O código original em C foi reescrito e modularizado em Pascal, utilizando `PROCEDURE`s e `FUNCTION`s para separar a lógica de I/O da lógica de cálculo.

### 🗝️ Conceitos Chave Aplicados

* **Procedures vs. Functions:** Diferenciação clara entre os tipos de sub-rotinas e sua aplicação correta.
* **Escopo de Variáveis:** Gerenciamento das variáveis locais e globais dentro da arquitetura de Pascal.

### 📂 Arquivos Chave

* **Código Fonte:** Arquivos de código-fonte Pascal (`.pas`) contendo a solução dos problemas da Lista 3.
* **Enunciado:** Arquivo `Lista03-ParadigmaImperativo (Pascal)-2.pdf` com o problema de conversão (C para Pascal) e os requisitos de modularização.
